# Собранный артефакт публикуется намеренно: GitHub Pages отдаёт его как есть,
# и он же — то, что скачивают и открывают двойным кликом. НЕ игнорируем.

# служебное
.claude/settings.local.json
*.log

# временные файлы редакторов и ОС
Thumbs.db
desktop.ini
*~
.DS_Store
