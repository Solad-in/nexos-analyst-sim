# Единые окончания строк: в репозитории LF, в рабочей копии — родные для системы.
# Без этого файл, собранный на Windows, даёт шумный diff у любого, кто работает не на ней.
* text=auto eol=lf

# .ps1 запускается Windows PowerShell — ему нужны CRLF и BOM (см. build.ps1)
*.ps1 text eol=crlf